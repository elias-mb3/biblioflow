# Changelog

## [Unreleased]

All implementation below was completed in a single Claude Code session on **2026-05-19**
and is not yet committed to version control.

---

## Project Overview

**BiblioFlow** is a community library management REST API built for
Igreja Presbiteriana do Brasil (IPB) as a university extension project (UNASP —
Análise e Desenvolvimento de Sistemas). The goal is to digitalise and manage a
community book collection: registering books, controlling rentals (loans and
returns), and accepting donations.

**Tech stack:**

| Layer | Technology |
|---|---|
| Language | TypeScript (strict mode) |
| Runtime | Node.js 24 (project targets 20 LTS) |
| HTTP Framework | Express 5 |
| Database | SQLite — single file, Docker-volume-persisted |
| ORM | Prisma 7 + `@prisma/adapter-better-sqlite3` |
| Validation | Zod 4 |
| Auth | JWT (`jsonwebtoken`) + bcrypt |
| Tests | Vitest 4 + Supertest |
| Code quality | ESLint + Prettier |
| Containerisation | Docker + Docker Compose |

---

## Architecture

### Layered design: `routes → controllers → services → repositories`

The codebase is split into four strict layers:

- **`src/routes/`** — Express router definitions; mount middleware, call controllers.
- **`src/controllers/`** — Parse HTTP request, call the service, return the response.
  No business logic lives here.
- **`src/services/`** — All domain rules and business logic. The single source of
  truth for behaviour.
- **`src/repositories/`** — All database queries. No Prisma calls exist outside
  this layer.

Supporting directories:

- **`src/config/`** — `env.ts` (typed env vars) and `database.ts` (Prisma singleton).
- **`src/middlewares/`** — `authenticate`, `requireRole`, `validate`, `errorHandler`.
- **`src/schemas/`** — Zod schemas per resource; used by `validate` middleware.
- **`src/types/`** — Shared TypeScript interfaces (`AuthRequest`, `AuthPayload`).
- **`src/utils/`** — `AppError` class and `generateRegistrationCode` helper.

### Key architectural decisions

**1. English identifiers, Portuguese error messages**
Entity and field names use English (`fullName`, `periodDays`, `dueDate`) to
align with the broader dev community, while user-facing API error messages
remain in Portuguese to match the end-user context.

**2. `AppError` for controlled HTTP errors**
`src/utils/errors.ts` defines `AppError(message, statusCode, details?)`.
Services throw `AppError`; the global `errorHandler` middleware in
`src/middlewares/errorHandler.ts` catches it and serialises to
`{ "erro": "...", "detalhes": [...] }`. This keeps HTTP concerns out of
the service layer.

**3. Prisma 7 adapter pattern**
Prisma 7 no longer reads `DATABASE_URL` from the environment automatically at
runtime. The client must be constructed with an explicit driver adapter:

```ts
// src/config/database.ts
const adapter = new PrismaBetterSqlite3({ url: process.env.DATABASE_URL });
const prisma = new PrismaClient({ adapter });
```

`prisma.config.ts` (generated by `prisma init`) is used by the Prisma CLI only
(migrations, generate). The runtime client uses the adapter.

**4. Prisma 7 generated client path**
`prisma generate` outputs to `src/generated/prisma/` with no `index.ts`.
All imports must target the specific file:
```ts
import { PrismaClient } from '../generated/prisma/client';
import { Role, RentalStatus } from '../generated/prisma/client';
```

**5. Prisma singleton via `globalThis`**
To avoid creating multiple database connections during hot-reload in
development, the Prisma client is cached on `globalThis`:

```ts
const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };
export const prisma = globalForPrisma.prisma ?? createPrisma();
if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;
```

**6. `registrationCode` is server-generated and immutable**
`src/utils/registrationCode.ts` generates a code in the format
`BIB-<timestamp_base36>-<6_random_hex>` (e.g. `BIB-LKZP3A4-C9F2E1`).
The `createBookSchema` in `src/schemas/book.ts` intentionally does **not**
include a `registrationCode` field, so even if the client sends one, it is
stripped by `req.body = result.data` in the `validate` middleware.

**7. Zod 4 API change**
Zod v4 renamed `ZodError.errors` to `ZodError.issues`. All validation
error mappings use `.issues`:
```ts
result.error.issues.map(...)   // validate.ts
err.issues.map(...)            // errorHandler.ts
```

**8. Vitest `fileParallelism: false` for SQLite**
SQLite allows only one writer at a time. Running test files in parallel
(Vitest's default) causes `"attempt to write a readonly database"` errors
when multiple workers open the same `.db` file simultaneously. Setting
`fileParallelism: false` in `vitest.config.ts` serialises test file execution
within a single process, eliminating the conflict.

**9. Login identifier auto-detection**
`authService.login` detects whether the identifier is an email or CPF by
checking for `@`, avoiding a separate login endpoint per role:
```ts
input.identifier.includes('@')
  ? userRepository.findByEmail(...)
  : userRepository.findByCpf(...)
```

---

## Features

### Authentication — `src/routes/auth.ts`

| Method | Route | Auth | Description |
|---|---|---|---|
| POST | `/api/v1/auth/register` | public | Register MANAGER or USER |
| POST | `/api/v1/auth/login` | public | Authenticate, receive JWT |

**MANAGER** requires `email`; **USER** requires `cpf`. Enforced via
`z.superRefine` in `src/schemas/auth.ts`. Duplicate email/CPF returns `409`.
Passwords are hashed with bcrypt (10 rounds). JWT payload: `{ sub, role }`.

### Books — `src/routes/books.ts`

| Method | Route | Auth | Description |
|---|---|---|---|
| GET | `/api/v1/books` | 🔒 any | Paginated list (10/page) |
| GET | `/api/v1/books/search` | 🔒 any | Search by `title`, `author`, or `registrationCode` |
| GET | `/api/v1/books/:id` | 🔒 any | Get single book |
| POST | `/api/v1/books` | 👤 MANAGER | Create book (auto-generates `registrationCode`) |
| PUT | `/api/v1/books/:id` | 👤 MANAGER | Update book fields |
| DELETE | `/api/v1/books/:id` | 👤 MANAGER | Delete book (blocked if active rentals exist) |

Business rules enforced in `src/services/book.service.ts`:
- `registrationCode` generated on create; never accepted from client.
- Cannot delete a book with `status = ACTIVE` rentals → `409`.
- List returns `{ items, total, page, pages }`.

### Rentals — `src/routes/rentals.ts`

| Method | Route | Auth | Description |
|---|---|---|---|
| POST | `/api/v1/rentals` | 👤 MANAGER | Create rental order |
| GET | `/api/v1/rentals` | 👤 MANAGER | List rentals (filter by `status`) |
| GET | `/api/v1/rentals/pending` | 👤 MANAGER | List overdue active rentals |
| GET | `/api/v1/rentals/:id` | 👤 MANAGER | Get rental detail with book and user |
| PATCH | `/api/v1/rentals/:id/finalize` | 👤 MANAGER | Finalize (return) a rental |

Business rules enforced in `src/services/rental.service.ts`:
- **Valid periods:** `15`, `30`, `45` days only. Any other value → `400`.
  Enforced via `z.refine` in `src/schemas/rental.ts`.
- **Stock check:** `activeRentals >= book.quantity` → `409`.
- **User rental limit:** `activeRentals >= MAX_RENTALS_PER_USER` → `409`.
  Limit is configured via env var `MAX_RENTALS_PER_USER` (default: `3`).
- **`dueDate` calculation:** `dueDate = startDate + periodDays`.
- **Late detection on finalize:** `returnDate > dueDate` sets `late = true`.
- **Double-finalize guard:** Finalizing an already `FINALIZED` rental → `409`.
- **Pending list:** queries `status = ACTIVE AND dueDate < NOW()`, ordered
  by `dueDate ASC`.

### Donations — `src/routes/donations.ts`

| Method | Route | Auth | Description |
|---|---|---|---|
| POST | `/api/v1/donations` | 🔒 any | Register a book donation |

Donation records the donor (`userId`), `title`, and `author`. The `bookId`
field is optional (the book may not yet exist in the catalogue).
Validated that the user exists before creating.

---

## Data Model

Defined in `prisma/schema.prisma`. All tables use UUID primary keys.

```
User         — id, role (MANAGER|USER), fullName, phone, cpf?, email?,
               passwordHash, createdAt
Book         — id, registrationCode (unique), title, description, author,
               quantity, createdAt
Rental       — id, bookId→Book, userId→User, periodDays, startDate, dueDate,
               returnDate?, status (ACTIVE|FINALIZED), late, createdAt
Donation     — id, bookId?→Book, userId→User, title, author, createdAt
```

---

## Test Suite

**22 tests — 3 files — all passing.**

| File | Coverage |
|---|---|
| `tests/auth.test.ts` | Register (MANAGER/USER), validation rejection, duplicate detection, login success, wrong password |
| `tests/books.test.ts` | CRUD, pagination, search, role guard, 401 without token, safe delete |
| `tests/rentals.test.ts` | Create with valid/invalid period, stock exhaustion, user limit, finalize, double-finalize, pending list |

Test infrastructure (`tests/setup.ts`):
- Sets `DATABASE_URL=file:./data/test.db` at the top level (before any module
  import) so the Prisma singleton picks it up.
- Runs `prisma migrate deploy` in `beforeAll` to create the test schema.
- Deletes the test DB file in `afterAll`.

`tests/helpers.ts` provides `createManagerAndLogin()`, `createUserAndLogin()`,
and `seedBook()` to reduce boilerplate across test files.

---

## Infrastructure

### Docker

`Dockerfile` uses a two-stage build:
1. **builder** — installs all deps, compiles TypeScript to `dist/`.
2. **runner** — installs prod deps only, copies `dist/` and `prisma/`.

Container startup runs `prisma migrate deploy` before `node dist/server.js`.

`docker-compose.yml` mounts a named volume `db-data:/app/data` so
`biblioflow.db` survives container recreation.

### Environment variables

| Variable | Default | Description |
|---|---|---|
| `PORT` | `3000` | HTTP port |
| `NODE_ENV` | `development` | Execution environment |
| `DATABASE_URL` | `file:./data/biblioflow.db` | SQLite file path |
| `JWT_SECRET` | *(must be set)* | Token signing secret |
| `JWT_EXPIRES_IN` | `1d` | Token lifetime |
| `MAX_RENTALS_PER_USER` | `3` | Simultaneous rental limit per user |

---

## Commit History

### 2026-05-19 — 73bd0ad — Update README.md

- Replaced the one-line placeholder with the full project README.
- Added domain specification: entities, business rules, endpoint table,
  architecture diagram, and implementation roadmap.
- README written in Portuguese; serves as the domain source of truth.

### 2026-02-17 — cd9c2d8 — Initial commit

- Created repository with MIT licence, standard Node `.gitignore`, and
  a one-line README placeholder.

---

## Session Work (2026-05-19, uncommitted)

The following was implemented in a single Claude Code session and has not yet
been committed to git.

### Setup & tooling
- Initialised `package.json`; set `"type": "commonjs"`.
- Installed runtime deps: `express`, `prisma`, `@prisma/client`,
  `@prisma/adapter-better-sqlite3`, `better-sqlite3`, `zod`, `jsonwebtoken`,
  `bcrypt`, `dotenv`.
- Installed dev deps: `typescript`, `tsx`, `vitest`, `supertest`, `eslint`,
  `prettier`, `@typescript-eslint/*`.
- Configured `tsconfig.json` (target ES2022, strict), `.eslintrc.json`,
  `.prettierrc`.
- Added all `npm run` scripts: `dev`, `build`, `start`, `test`, `lint`,
  `db:migrate`, `db:seed`, `db:generate`, `db:studio`.

### Database
- Defined Prisma schema with four models: `User`, `Book`, `Rental`, `Donation`.
- Ran initial migration (`20260519215725_init`).
- Generated Prisma client to `src/generated/prisma/`.
- Created `prisma/seed.ts` with one MANAGER, one USER, and one sample book.
- Fixed: Prisma 7 requires `PrismaBetterSqlite3` adapter passed to constructor
  (see Architecture Decision #3).
- Fixed: all imports point to `../generated/prisma/client` not
  `../generated/prisma` (no `index.ts` generated in Prisma 7).

### Application core
- `src/server.ts` — bootstraps Express, starts HTTP listener.
- `src/app.ts` — registers middlewares and four routers under `/api/v1`.
- `src/config/env.ts` — typed env object with defaults.
- `src/config/database.ts` — Prisma singleton with `globalThis` cache.
- `src/types/index.ts` — `AuthRequest` and `AuthPayload` interfaces.
- `src/utils/errors.ts` — `AppError` with `statusCode` and `details`.
- `src/utils/registrationCode.ts` — generates `BIB-<base36>-<hex>` codes.

### Authentication
- `src/schemas/auth.ts` — `registerSchema` (superRefine for role-specific
  required fields), `loginSchema`.
- `src/repositories/user.repository.ts` — findByEmail, findByCpf, findById,
  create, countActiveRentals.
- `src/services/auth.service.ts` — register (duplicate check, bcrypt hash),
  login (identifier auto-detect, bcrypt compare, JWT sign).
- `src/controllers/auth.controller.ts` + `src/routes/auth.ts`.

### Books
- `src/schemas/book.ts` — createBookSchema (no `registrationCode` field),
  updateBookSchema, bookSearchSchema.
- `src/repositories/book.repository.ts` — list (paginated), search (dynamic
  `where` by field), findById, create, update, delete, countActiveRentals.
- `src/services/book.service.ts` — enforces delete guard and code generation.
- `src/controllers/book.controller.ts` + `src/routes/books.ts`.

### Rentals
- `src/schemas/rental.ts` — `z.refine` to enforce periods `[15, 30, 45]`.
- `src/repositories/rental.repository.ts` — list (with status filter),
  listPending (ACTIVE + dueDate < now), findById (includes book + user),
  countActive, countUserActive, create, finalize.
- `src/services/rental.service.ts` — stock check, user limit check, dueDate
  calculation, late detection on finalize, double-finalize guard.
- `src/controllers/rental.controller.ts` + `src/routes/rentals.ts`.

### Donations
- `src/schemas/donation.ts`, `src/repositories/donation.repository.ts`,
  `src/services/donation.service.ts`, controller and route.

### Middleware
- `src/middlewares/auth.ts` — `authenticate` (JWT verify) and
  `requireRole(role)` (role guard factory).
- `src/middlewares/validate.ts` — Zod `safeParse` on `req.body`; strips
  unknown fields; uses `.issues` (Zod v4 API).
- `src/middlewares/errorHandler.ts` — catches `AppError` and `ZodError`
  centrally; falls back to 500.

### Tests
- `tests/setup.ts` — sets env vars at module level, deploys migrations,
  cleans up test DB.
- `tests/helpers.ts` — shared `createManagerAndLogin`, `createUserAndLogin`,
  `seedBook`.
- `tests/auth.test.ts` — 7 tests covering registration, validation, and login.
- `tests/books.test.ts` — 8 tests covering CRUD, pagination, search, auth.
- `tests/rentals.test.ts` — 7 tests covering all rental business rules.
- Fixed: `vitest.config.ts` sets `fileParallelism: false` to prevent
  concurrent SQLite write errors across test files.
- **Result: 22/22 tests passing.**

### Docker & config
- `Dockerfile` (two-stage), `docker-compose.yml` (single service, named
  volume), `.dockerignore`, `.env.example`, `CLAUDE.md`.

### Statusline
- Created `~/.claude/statusline.sh`: shows folder, git branch, context bar,
  model name.
- Fixed: replaced `jq` dependency with `python3 -c` inline parsing (jq not
  installed in this environment).

### Skills
- Created `~/.claude/skills/changelog/SKILL.md`: documents how to generate
  this file on demand with `/changelog`.
